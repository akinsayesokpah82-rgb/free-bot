# Copy to .env locally for development only (do not commit .env)
# For Render, set these keys in the Render dashboard as environment variables.

PORT=3000
NODE_ENV=production
OPENAI_API_KEY=sk-REPLACE-ME
JWT_SECRET=replace_with_random_secret
RATE_LIMIT_PER_MINUTE=120
